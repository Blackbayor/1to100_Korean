# 작업 로그

## 2025년 9월 1일

### 1. 프로젝트 구조 정리
- `scripts`, `archive` 폴더를 신규 생성했습니다.
- 루트 폴더에 있던 유틸리티 스크립트들을 `scripts` 폴더로 이동시켰습니다.
- 지난 학습 결과물(`runs` 폴더)을 `archive` 폴더로 백업했습니다.
- 가장 성능이 좋은 모델(`best.pt`)들을 `models` 폴더로 이동하여 관리하도록 했습니다.

### 2. 모델 성능 평가
- `yolo_runs_best.pt`와 `yolo_notebooks_best.pt` 두 모델의 성능을 평가했습니다.
- 전체 성능(mAP)을 비교한 결과, `yolo_runs_best.pt`를 최종 모델로 선정했습니다.

### 3. API 서버 구축 및 파이프라인 완성
- FastAPI를 사용하여 `src/api.py`에 API 서버를 구축했습니다.
- **`/predict` API**: PDF 또는 이미지를 받아 객체 탐지 결과를 JSON으로 반환하는 기능을 구현했습니다.
- **`/recombine` API**: 프로젝트의 핵심 기능인 'PDF 재조합' 파이프라인을 완성했습니다.
  - PDF 업로드 -> 객체 탐지 -> 지문/문제 그룹핑 -> 순서 섞기 -> 새 PDF 생성 및 다운로드의 전 과정을 자동화했습니다.

### 4. 버그 수정 및 안정화
- **`KeyError: 'label'`**: `layout_organizer`와 `pdf_recombiner`에서 발생하던 키 오류를 해결했습니다.
- **`FileNotFoundError`**: PDF 생성 후 임시 파일이 먼저 삭제되던 문제를 `BackgroundTask`를 이용해 해결했습니다.
- **`SyntaxError`**: API 코드의 오타를 수정했습니다.
- **DPI 및 크기 문제**: 모델 학습 환경(72 DPI)과 추론 환경의 DPI가 달라 이미지가 비정상적으로 크게 보이던 문제를 해결했습니다.

### 5. 현재 상태 및 다음 작업
- 현재 재조합된 PDF의 구성요소 크기가 여전히 너무 크다는 피드백이 있습니다.
- 다음 작업 시, 이 레이아웃 문제를 해결하는 것을 최우선 과제로 삼겠습니다.
